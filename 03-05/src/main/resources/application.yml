spring:
  jpa:
    # 전송 쿼리 확인
    show-sql: true # 애플리케이션 실행시, 데이터베이스 쿼리 실행 구문을 모두 보여주는 옵션
    properties:
      hibernate:
        format_sql: true

    # 테이블 생성 후에 data.sql 실행
    defer-datasource-initialization: true # 애플리케이션 실행시, 테이블을 생성하고 data.sql파일에 있는 쿼리를 실행하도록 하는 옵션